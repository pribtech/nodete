SELECT 'MDC' AS TYPE, A.TABSCHEMA AS SCHEMA, A.TABNAME AS NAME
FROM SYSCAT.INDEXES A, SYSCAT.TABLES B
WHERE (A.TABNAME=B.TABNAME
AND A.TABSCHEMA=B.TABSCHEMA)
AND A.INDEXTYPE = 'BLOK'
UNION ALL
SELECT 'MQT' AS TYPE, TABSCHEMA AS SCHEMA, TABNAME AS NAME
FROM SYSCAT.TABLES
WHERE TYPE = 'S'