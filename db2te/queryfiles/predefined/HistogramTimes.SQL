 SELECT DISTINCT HISTOGRAM_TYPE,
         PARENTSERVICECLASSNAME,
         SERVICECLASSNAME,
         SERVICECLASSID,
         STATISTICS_TIMESTAMP
  FROM HISTOGRAMBIN_DB2STATISTICS AS H,
       SYSCAT.SERVICECLASSES AS S
  WHERE H.SERVICE_CLASS_ID = S.SERVICECLASSID 