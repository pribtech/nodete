SELECT row_number() 
OVER() as IDNUM, POOL_ID, 
SUM(DECIMAL((POOL_CUR_SIZE/1024.0/1024.0),10,2)) AS CUR_MB, 
SUM(DECIMAL((POOL_WATERMARK/1024.0/1024.0),10,2)) AS WATERMARK_MB
FROM SYSIBMADM.SNAPDB_MEMORY_POOL 
	GROUP BY POOL_ID
	ORDER BY CUR_MB